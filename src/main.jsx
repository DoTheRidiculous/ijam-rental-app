import React from "react";
import ReactDOM from "react-dom/client";
import RentalApplicationApp from "./RentalApplicationApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RentalApplicationApp />
  </React.StrictMode>
);
